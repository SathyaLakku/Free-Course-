# Backend - Free Course Locator

## Setup

```bash
npm install
npm start
```

- Runs on [http://localhost:5000](http://localhost:5000)
- API Endpoints:
  - `/api/courses?q=search&domain=Computer Science`
  - `/api/domains` (for filter dropdown)